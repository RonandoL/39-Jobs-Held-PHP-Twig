<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <title>Jobs</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
  </head>

  <body>

    <div class='container'>
      <div class='row'>
        <div class='col-md-4'>
          <h2>My Job History</h2>

          <form action='/jobs' method='post'>
            <div class='form-group'>
              <label for='title'>Enter Job Title Here</label>
              <input type='text' name='title' id='title' class='form-control' placeholder='Sniper'>
            </div>
            <button type='submit' class='btn btn-sm btn-danger'>Submit Job Title</button>
          </form>

          <br>   <!-- DELETE BUTTON BITCH! -->
          <form action='/delete' method='post'>
            <button type='submit' class='btn btn-sm'>Delete All Jobs</button>
          </form>

        </div>  <!-- .col-md-3 -->
      </div>  <!-- .row -->

      <div class='row'>
          <div class='col-md-3'>
              <hr>
              {% if jobs is not empty %}
                  <h4>sfsd sdf sdf ff</h4>
                  <ul>
                  {% for job in jobs %}
                      <li>{{ job.getTitle }}</li>
                  {% endfor %}
                  </ul>
              {% endif %}
          </div>
      </div>  <!-- .row -->
    </div>  <!-- .container -->

  </body>






</html>
